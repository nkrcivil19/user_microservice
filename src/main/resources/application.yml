server:
  port: 8001
spring:
  application:
    name: 
  datasource:
   url: jdbc:postgresql://localhost:5432/userservice
   username: postgres
   password: root
  jpa:
   show-sql: true
   hibernate:
      ddl-auto: update 